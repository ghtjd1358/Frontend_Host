/**
 * Host Route Paths
 * /container prefix 사용
 */
export enum RoutePath {
    Dashboard = '/container/dashboard',
    Resume = '/container/resume',
    Blog = '/container/blog',
    Login = '/login',
}
