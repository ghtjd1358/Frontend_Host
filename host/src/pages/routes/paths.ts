/**
 * Host Route Paths
 */
export const platformPrefix = '/platform';

export enum RoutePath {
    // Platform
    Dashboard = '/dashboard',
    Resume = '/resume',
    Blog = '/blog',

    // Auth
    Login = '/login',
}
